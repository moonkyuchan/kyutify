import React from "react";
import styled from "styled-components";

const PlayInfo = () => {
  return <PlayInfoBlock></PlayInfoBlock>;
};

const PlayInfoBlock = styled.div`
  width: 25%;
  height: 90px;
  border: 0px solid black;
  background: pink;
`;

export default PlayInfo;
